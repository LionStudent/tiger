To run the code, you will need to download matplotlib. You can do this by pasting the code below into the terminal:

cd matplotlib

python -m pip install --upgrade pip --user

python -m pip install -r requirements.txt